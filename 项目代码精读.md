# 可转债日报 · 项目代码精读（初学者向）

> 这份文档按"由外到内、由配置到代码"的顺序，把项目里**每一个文件**拆开讲透：它是什么、为什么需要它、关键代码每一行做什么。
>
> 读完之后你应该能回答：
> - 这个项目怎么从一堆 JSON 数据变成你看到的网页？
> - 为什么要分这么多文件，而不是塞进一个 HTML 里？
> - 每一个文件解决了什么"如果不这样会怎样"的问题？

---

## 0. 先看全景：项目是什么

一句话：**一个每天自动更新数据的可转债市场日报单页网站，托管在 GitHub Pages 上。**

```
                ┌──────────────────────┐
                │  腾讯云 CloudBase     │
                │  云函数（别人写的）   │
                └──────────┬───────────┘
                           │ 每个工作日 15:15 推送
                           ▼
                ┌──────────────────────┐
                │  data.json           │  ← 数据
                │  index.html          │  ← 页面 + 渲染逻辑
                │  styles.css          │  ← 样式（构建产物）
                └──────────┬───────────┘
                           │ git push
                           ▼
                ┌──────────────────────┐
                │  GitHub Pages         │
                │  https://morecori…    │  ← 用户访问的网址
                └──────────┬───────────┘
                           │ 浏览器打开
                           ▼
                  用户看到日报页面
```

**用户访问时实际发生的事**：
1. 浏览器请求 `index.html`，得到一个几乎空白的页面骨架 + 一段 JS
2. JS 自动发请求拉 `data.json`
3. JS 把数据"翻译"成 HTML（标题、表格、卡片…），塞进 `#app` 容器
4. 用户看到完整日报

所以这个项目的核心就是：**怎么用一段 JS 把 JSON 数据漂亮地渲染成网页**。

---

## 1. 文件清单与各自职责

| 文件 | 类型 | 大小 | 职责 | 你需要读懂吗 |
|------|------|------|------|------|
| `package.json` | 配置 | 15 行 | 告诉 npm 这是啥项目、有哪些依赖、有哪些自定义命令 | ✅ 必读 |
| `src/app.css` | 源码 | 92 行 | **Tailwind v4 的入口**：`@theme` 写设计 token + `@custom-variant` 定义断点 + `@layer components` 写复杂组件 | ✅ 必读 |
| `styles.css` | 产物 | 12KB | `src/app.css` 编译后的产物，浏览器真正加载的 CSS | ❌ 跳过 |
| `index.html` | 源码 | 17 行 | 页面 HTML 骨架，只负责结构，不含 JS 逻辑 | ✅ 必读 |
| `app.js` | 源码 | 280 行 | **核心 JS**：所有渲染逻辑（模板函数 + 数据加载） | ✅ 必读（重点） |
| `data.json` | 数据 | ~14KB | 当天的市场数据，由云函数推送 | 看结构即可 |
| `.gitignore` | 配置 | 3 行 | 告诉 git 哪些文件不要提交 | ✅ 简单 |
| `README.md` | 文档 | — | 给人看的项目说明 | 跳过 |

下面逐个讲。

---

## 2. `package.json` —— 项目的"身份证"

### 完整代码

```json
{
  "name": "cbreport-web",
  "version": "1.0.0",
  "description": "可转债日报 Web 版 - Tailwind CSS v4 重构",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/app.css -o ./styles.css --minify",
    "dev": "tailwindcss -i ./src/app.css -o ./styles.css --watch",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
```

### 字段逐个讲

- **`name`**：项目名。npm 包的命名规范（小写、无空格）。这里只是给本地项目起个名字，不会真的发布到 npm。
- **`version`**：语义化版本（Semantic Versioning，semver）。`主版本.次版本.修订号`。初学项目写 `1.0.0` 即可。
- **`description`**：一句话描述，给人看的。
- **`private`**: `true` 表示"私有项目，禁止 `npm publish` 上传到 npm 公共仓库"。这是个静态网站，不需要发布成 npm 包。
- **`scripts`**: ⭐ 这是最实用的字段。它把一长串命令起个短名字，让你用 `npm run xxx` 调用。
  - `npm run build` → 实际执行 `tailwindcss -i ./src/app.css -o ./styles.css --minify`
    - `-i` 输入文件，`-o` 输出文件，`--minify` 压缩
    - 作用：把 `src/app.css` 编译成浏览器能用的 `styles.css`
  - `npm run dev` → 加了 `--watch`，监听文件变化自动重建。开发时一直开着。
  - `npm run serve` → 用 Python 自带的 http 服务器在 8000 端口起服务。**为啥不直接双击 index.html？** 因为浏览器禁止 `file://` 协议发 `fetch` 请求（CORS 安全策略），必须用 `http://` 才能正常加载 `data.json`。
- **`devDependencies`**：开发依赖。"开发"的意思是：只在编写/构建时需要，**运行时（浏览器打开页面）不需要**。
  - `tailwindcss` 是 v4 引擎本体（Rust 实现 + JS API），用来编译 CSS
  - `@tailwindcss/cli` 是 v4 的命令行工具包——v4 把 CLI 拆成独立包了，v3 是合在 `tailwindcss` 里的
  - 两者都需要装，缺一个 `npm run build` 都跑不起来
  - `^4.3.3` 表示"允许 4.x.x 的最新版本，但不升到 5.0"。`^` 是 semver 的兼容性约定

### 为什么需要这个文件

1. **可复现**：别人 clone 你的仓库后跑 `npm install`，npm 会读 `package.json`，自动装上正确版本的依赖。
2. **命令别名**：不用记 `tailwindcss -i ... -o ... --minify` 这种长命令。
3. **配合 `package-lock.json`**（自动生成的）：锁定每个依赖的确切版本，保证你、我、CI 服务器装出来的依赖完全一致。

---

## 3. `src/app.css` —— Tailwind v4 入口（设计系统 + 组件层）

这个文件是 v4 风格的"单文件入口"——把 v3 时代的两个文件（`tailwind.config.js` 配置文件 + `src/input.css` CSS 入口）合并成一个。

### 这个文件解决什么问题

普通 Tailwind 类如 `text-red-500` 是内置的。但本项目有自己的品牌色（紫色 `#8250DF`）、涨跌色（红涨绿跌），如果到处写 `<span style="color:#8250DF">` 就回到老路了。

这个文件让你**给颜色起名字**：在 `@theme` 块里定义一次 `--color-brand: #8250DF`，整个项目里就能写 `text-brand`、`bg-brand`、`border-brand`，跟用内置颜色一样方便。

同时它还负责把"工具类搞不定的复杂样式"（如隐藏滚动条、bar 图组件）抽成 `.table-wrapper`、`.bar-track` 这种简短类名。

### 完整代码（分 3 段）

#### 第 1 段：导入 + 主题配置 `@theme`

```css
@import "tailwindcss";

@theme {
  /* 品牌主色：紫色（带 50~900 色阶） */
  --color-brand-50: #F5F0FD;
  --color-brand-100: #EBE0FB;
  /* ... 200~400 略 */
  --color-brand-500: #8250DF;
  /* ... 600~900 略 */
  /* 不带数字时直接 text-brand / bg-brand → 用 500 */
  --color-brand: var(--color-brand-500);

  /* 涨跌色（A股惯例：红涨绿跌） */
  --color-up: #FF4757;
  --color-down: #2ED573;
  --color-gold: #FDCB6E;

  /* 文本层级 */
  --color-ink: #2D3436;
  --color-ink-medium: #636E72;
  --color-ink-light: #B2BEC3;

  /* 卡片 / 边框 / 分隔线 / 页面背景 */
  --color-surface: #FFFFFF;
  --color-surface-alt: #F8F9FA;
  --color-surface-border: #E8E8E8;
  --color-surface-separator: #E0E0E0;
  --color-surface-page: #F1F2F6;

  /* 字体栈 */
  --font-sans: "Noto Sans CJK SC", "PingFang SC", "WenQuanYi Micro Hei", "Microsoft YaHei", sans-serif;
  --font-mono: "DejaVu Sans Mono", "Consolas", monospace;
  --font-script: "Segoe Script", "Palatino Linotype", Georgia, serif;

  /* 最大宽度容器 */
  --max-width-report: 750px;
}
```

#### 第 2 段：自定义断点 `@custom-variant`

```css
@custom-variant mobile (@media (max-width: 600px));
@custom-variant tiny (@media (max-width: 380px));
```

#### 第 3 段：组件层 `@layer components`

```css
@layer components {
  .table-wrapper {
    @apply bg-white border border-surface-border rounded-lg overflow-x-auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }
  .table-wrapper::-webkit-scrollbar { display: none; }
  .table-wrapper tbody tr:last-child td { border-bottom: none; }

  .bar-track {
    @apply absolute bottom-0.5 left-1 right-1 h-1 bg-white rounded-sm overflow-hidden;
  }
  .bar-track-bidi {
    @apply absolute bottom-0.5 left-1 right-1 h-1 bg-white rounded-sm overflow-hidden flex justify-center;
  }
  .bar-fill { @apply h-full rounded-sm; }
  .bar-right { @apply h-full absolute left-1/2 rounded-r-sm; }
  .bar-left { @apply h-full absolute right-1/2 rounded-l-sm; }
}
```

### 关键概念逐个讲

#### `@import "tailwindcss"` —— v4 的一行取代 v3 的三行

v3 要写三行 `@tailwind base/components/utilities`，v4 只需一行 `@import "tailwindcss"`。这一行同时完成三件事：

1. 注入 **preflight**（重置浏览器默认样式：清掉 `h1`、`p` 的默认 margin、字号等，让所有浏览器起点一致）
2. 注入 `@theme` 块里你定义的设计 token
3. 注入用到的所有工具类（`bg-white`、`text-brand` 等）

#### `@theme` —— v4 配置的核心指令

v3 在 `tailwind.config.js` 里写 JS 对象，v4 改成在 CSS 里写"CSS 变量声明"。**命名规则是关键**：

| v4 变量名 | 自动生成的工具类 |
|----------|----------------|
| `--color-brand` | `text-brand`、`bg-brand`、`border-brand` 等 |
| `--color-brand-500` | `text-brand-500`、`bg-brand-500` |
| `--font-sans` | `font-sans` |
| `--max-width-report` | `max-w-report` |

**为什么用 CSS 变量而不是 JS 对象？**

1. **运行时可改**：打开浏览器 DevTools，直接改 `:root { --color-brand: #0000FF }`，页面所有 `text-brand` 立刻变蓝，无需重建。v3 做不到。
2. **DevTools 可见**：在 Elements 面板能看到 `--color-brand` 这种变量名，调试方便。
3. **少一个文件**：不用维护单独的 `tailwind.config.js`。

#### `--color-brand` 和 `--color-brand-500` 的关系

```css
--color-brand-500: #8250DF;        /* 具体的色值 */
--color-brand: var(--color-brand-500);  /* 别名引用 */
```

这样 `text-brand` 和 `text-brand-500` 都能用，且 `text-brand` 默认指向 500 色。后续要换默认色，只改 `--color-brand` 的引用，不动 `--color-brand-500`。

#### `@custom-variant` —— v4 自定义断点的新写法

v4 默认断点（`sm:` `md:` `lg:`）都是 `min-width`（移动优先）。本项目沿用 `max-width`（桌面优先）写法，所以要用 `@custom-variant` 显式声明：

```css
@custom-variant mobile (@media (max-width: 600px));
```

**注意语法**：`(@media (max-width: 600px))` 整个 `@media ...` 表达式要用**括号包起来**。这是 v4 容易踩的坑——写成 `&media (...)` 或 `media (...)` 都会报错。

定义后 `mobile:text-[21px]`、`tiny:px-2` 等工具类就自动生效，和内置断点用法完全一样。

#### `@layer components` 是什么

`@layer` 是 CSS 原生特性，叫"层叠层"（cascade layer）。Tailwind 用它控制**优先级**：

- `theme` 层：你的设计 token（CSS 变量）
- `base` 层：重置样式（优先级最低）
- `components` 层：自定义组件类（你写的 `.table-wrapper`）
- `utilities` 层：原子工具类（`bg-white` 等，优先级最高）

好处：你写 `<div class="table-wrapper bg-red-500">`，`bg-red-500` 一定能覆盖 `.table-wrapper` 里的背景色，因为 `utilities` 层永远赢过 `components` 层。**这就是 Tailwind 工具类能"指哪打哪"的奥秘。**

#### `@apply` —— 在 CSS 里复用工具类

```css
.table-wrapper {
  @apply bg-white border border-surface-border rounded-lg overflow-x-auto;
  /* 等价于：
     background-color: white;
     border: 1px solid var(--color-surface-border);
     border-radius: 0.5rem;
     overflow-x: auto;
  */
}
```

`@apply` 让你"在 CSS 里借用工具类"。本质是把那一坨工具类的 CSS 复制粘贴过来。

#### 哪些用 `@apply`，哪些用原生 CSS？

- 能用 `@apply` 表达的，都用 `@apply`（统一风格）
- `@apply` 表达不了的（伪元素 `::-webkit-scrollbar`、子选择器 `tbody tr:last-child td`），用原生 CSS

#### ⚠️ v4 的一个新限制：`@apply` 不能引用同层自定义类

```css
/* ❌ v4 报错：Cannot apply unknown utility class `bar-track` */
.bar-track-bidi {
  @apply bar-track flex justify-center;
}

/* ✅ v4 必须展开写 */
.bar-track-bidi {
  @apply absolute bottom-0.5 left-1 right-1 h-1 bg-white rounded-sm overflow-hidden flex justify-center;
}
```

v3 允许 `@apply` 引用同 `@layer components` 里的另一个自定义类（组件化复用），v4 禁止了。**为什么？** 因为 v4 的工具类解析器不知道 `.bar-track` 是"工具类"还是"普通 CSS 类"——它只认 `@theme` 里定义的东西。这是 v4 为了"工具类纯净"做的取舍。

代价：`.bar-track-bidi` 要重复写一遍 `.bar-track` 的内容。本项目这点重复可以接受。

### 设计哲学：把"设计系统"集中在一处

颜色、字体、断点、组件类**全部集中在 `src/app.css`**。改品牌色？只改一处 `--color-brand` 的值。这是大型项目维护的核心理念：**单一数据源（Single Source of Truth）**。

---

## 4. `styles.css` —— 构建产物（不用读懂）

这个文件是 `npm run build` 生成的。它的工作就是把上面 `src/app.css` 加上 `index.html` 里用到的所有工具类，**全部塞进一个压缩文件**。

### 为什么要把产物提交到仓库

正常项目 `.gitignore` 会忽略构建产物。但这里**特殊**：

- GitHub Pages 是纯静态托管，**没有构建步骤**
- 它只是把仓库里的文件原样伺服给浏览器
- 如果不提交 `styles.css`，浏览器请求会 404

所以这个项目的约定是：**改完 CSS 必须本地跑 `npm run build` 再提交**。

### v4 产物的特征：CSS 变量

打开 `styles.css` 你会发现自定义颜色都成了 CSS 变量引用：

```css
/* v3 产物（静态值，运行时不能改） */
.text-brand { color: rgb(130 80 223 / var(--tw-text-opacity, 1)); }

/* v4 产物（变量引用，运行时可改） */
.text-brand { color: var(--color-brand); }
```

打开浏览器 DevTools，在 `:root` 上能看到 `--color-brand`、`--color-up` 等所有 token。改它们的值，页面所有用到的地方立刻跟着变——这是 v3 做不到的。

### 你应该读懂吗

不应该。它是机器生成的、压缩过的、一坨。**永远不要手改这个文件**，改了也会被下次构建覆盖。所有样式源头都在 `src/app.css` 一个文件里。

---

## 5. `.gitignore` —— 告诉 git 哪些文件不要提交

```
node_modules/    # npm 装的依赖，几千个文件、几百 MB，clone 时让 npm install 自己装
*.log             # 各种日志文件
.DS_Store         # macOS 系统自动生成的垃圾文件
```

### 为什么 `node_modules/` 不能提交

1. **太大**：一个简单项目几百 MB，会让仓库膨胀
2. **跨平台**：某些依赖会编译针对你操作系统的二进制，提交上去别人 clone 下来用不了
3. **没必要**：`package.json` + `package-lock.json` 已经精确记录了该装什么，`npm install` 能 100% 还原

### `.gitignore` 的工作方式

每行一条规则。`*` 是通配符。`/` 结尾表示只匹配目录。

```
node_modules/    → 忽略任何位置的 node_modules 目录
*.log             → 忽略任何 .log 后缀的文件
.DS_Store        → 忽略任何位置的 .DS_Store 文件
```

---

## 6. `data.json` —— 数据源

只看结构，不用看具体数值。

```json
{
  "tradeDate": "2026-07-24",
  "generateTime": "2026-07-24 15:25",
  "bondCount": 307,
  "market": {
    "up": 44, "down": 263, "flat": 0,
    "totalAmount": "664.88",
    "top10Gain": "216.37", "top10GainPct": "32.5%",
    "top20Gain": "276.27", "top20GainPct": "41.6%",
    "priceMedian": "131.471",
    "gainMedian": "-0.78%",
    "amountMedian": "0.55"
  },
  "tables": {
    "priceDist":     { "colLabels": [...], "rows": [[...], ...], "barColIndices": [...] },
    "industryDist":  { ... },
    "topGainers":    { ..., "subtitle": "榜单总成交额: 216.37亿" },
    "topAmount":     { ..., "subtitle": "..." },
    "topLosers":     { ..., "subtitle": "..." }
  }
}
```

### 设计要点

- **扁平结构**：尽量少嵌套，方便 JS 取值（`data.market.up` 而不是 `data.market.summary.up_count`）
- **数据已是字符串**：注意 `up: 44` 是数字但 `top10Gain: "216.37"` 是字符串。**为什么？** 因为这些值最终要直接显示在页面上，云函数已经格式化好（保留几位小数、加 `%`、加"亿"等），前端不用再算。
- **`tables.xxx` 结构一致**：每张表都有 `colLabels`（表头）+ `rows`（数据行）+ 可选 `barColIndices`（哪几列要画 bar 图）。结构一致的好处：**一套渲染函数 `tableTpl` 通吃所有表**。

---

## 7. `index.html` + `app.js` —— 页面骨架与渲染逻辑（重点！）

这是项目的"灵魂"。原本 JS 嵌在 HTML 里，现在拆成两个文件：
- **`index.html`**（17 行）：页面骨架，只负责结构
- **`app.js`**（280 行）：所有渲染逻辑（模板函数 + 数据加载）

**为什么拆开**？看本节末尾"为什么把 JS 从 HTML 拆出来"小节。

### 7.1 `index.html` —— 页面骨架

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>可转债日报</title>
<link rel="stylesheet" href="styles.css">
<script src="app.js" defer></script>
</head>
<body class="bg-surface-page text-ink font-sans antialiased leading-relaxed">
<div class="mx-auto max-w-report px-6 pt-[30px] pb-[60px] mobile:px-2.5 mobile:pt-3.5 mobile:pb-8 tiny:px-2 tiny:pt-2.5 tiny:pb-7">
  <div id="app">
    <div class="text-center py-16 text-ink-light text-base">正在加载数据...</div>
  </div>
</div>
</body>
</html>
```

#### 逐行讲

- `<!DOCTYPE html>` —— 告诉浏览器"这是 HTML5 文档"。不写的话浏览器会进入"怪异模式"，盒模型、字号都会变。
- `<html lang="zh-CN">` —— `lang` 属性告诉搜索引擎和读屏软件这是中文页面。无障碍 + SEO 友好。
- `<meta charset="UTF-8">` —— 字符编码。不写中文可能乱码。
- `<meta name="viewport" ...>` —— 让手机端正常显示。不写手机会把页面缩成 PC 宽度再缩小，字小到看不清。
- `<title>可转债日报</title>` —— 浏览器标签页上显示的文字。
- `<link rel="stylesheet" href="styles.css">` —— 引入 CSS。
- `<script src="app.js" defer></script>` —— 引入 JS。**两个关键点**：
  - `src="app.js"` 让 JS 从外部文件加载
  - `defer` 告诉浏览器："**等 HTML 解析完再执行 JS**"。没有 `defer` 的话，浏览器遇到 `<script>` 会立刻执行，但此时 `<div id="app">` 可能还没被解析出来，`getElementById('app')` 会返回 `null` 报错
- `<body class="...">` —— body 上的 Tailwind 类是**全局默认**：
  - `bg-surface-page` 背景色
  - `text-ink` 默认文字色
  - `font-sans` 默认字体
  - `antialiased` 字体抗锯齿
  - `leading-relaxed` 行高 1.625，文字松一点更易读
- 外层 `<div class="mx-auto max-w-report ...">` —— 内容居中 + 最大宽度 750px + 响应式内边距
- `<div id="app">` —— **JS 注入内容的容器**。初始内容是"正在加载数据..."占位文字。

#### `defer` vs 不写 vs `async` 的区别

| 写法 | 何时执行 | 阻塞 HTML 解析吗 |
|------|---------|----------------|
| `<script src="x.js">` | 立刻下载+执行 | 是，下载和执行期间 HTML 都停 |
| `<script src="x.js" defer>` | HTML 解析完后，按顺序执行 | 否 |
| `<script src="x.js" async>` | 下载完就执行，顺序不保证 | 否 |

`defer` 最适合本项目：保证 `<div id="app">` 先被解析出来，JS 才执行。

### 7.2 `app.js` —— JS 整体结构

```js
(function() {
  'use strict';

  // 工具函数
  function esc(s) { ... }
  function gainColorClass(val) { ... }

  // 模板函数：每个返回一段 HTML 字符串
  function errorTpl(msg) { ... }
  function freshnessTpl(tradeDate) { ... }
  function headerTpl(data) { ... }
  function marketCardTpl(m) { ... }
  function tableTpl(tableData, colWidths, stickyCount) { ... }
  function moduleTpl(title, subtitle, content) { ... }

  // 主渲染：组合所有模板
  function renderPage(data) { ... }

  // 入口：拉数据，调渲染
  function loadData() { ... }

  loadData();   // 启动
})();
```

#### 为什么用 IIFE 包起来

```js
(function() { 'use strict'; /* ... */ })();
```

`(function(){...})()` 叫 **IIFE**（立即调用函数表达式）。

作用：把所有变量封在函数内部，**不污染全局**。如果不包：

```js
var COLOR = {...};        // 全局变量，会污染 window.COLOR
function esc(s){...}      // 全局函数，会污染 window.esc
```

包起来之后，`COLOR` 只在 IIFE 作用域内可见，外面访问不到，也不会和别的脚本打架。

#### `'use strict'` 是什么

启用 JavaScript 严格模式。在这种模式下：
- 没声明就赋值（`x = 5`）会报错，而不是悄悄创建全局变量
- 删不掉的属性硬删会报错
- 函数参数重名会报错

**总之：让 JS 的"坑"提前爆出来，而不是埋雷。** 现代项目都该开。

### 7.3 工具函数

#### `esc(s)` —— XSS 防御

```js
function esc(s) {
  if (s == null) return '';
  return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
}
```

把 `&` `<` `>` `"` 这 4 个 HTML 特殊字符替换成"实体"。

**为什么需要？** 我们渲染的是动态数据，如果某条数据里混进了 `<script>` 标签：

```js
// 假设 data.market.up 是 "<script>alert('hack')</script>"
'<span>' + data.market.up + '</span>'
// 结果：<span><script>alert('hack')</script></span>
// 浏览器会真的执行这段 script！
```

`esc()` 把 `<` 变成 `&lt;`，浏览器就只显示字面文字 `<script>` 而不执行。这叫 **HTML 转义**，是防 XSS（跨站脚本攻击）的基本功。

**所有动态数据拼进 HTML 之前，都必须过 `esc()`**。看代码里 `esc(val)`、`esc(data.tradeDate)` 全都包了，这是正确做法。

#### `gainColorClass(val)` —— 涨跌色映射

```js
function gainColorClass(val) {
  var n = parseFloat(String(val).replace('%', ''));
  if (isNaN(n)) return 'text-ink';
  if (n > 0) return 'text-up font-bold';
  if (n < 0) return 'text-down font-bold';
  return 'text-ink font-bold';
}
```

输入：`"+3.5%"`、`"-2.1%"`、`"0%"`
输出：返回 Tailwind 类名字符串

**为什么返回类名而不是颜色值？** 因为我们要用 Tailwind 工具类（`text-up`）。返回类名后直接拼到 HTML 里：`<span class="... text-up font-bold">`。

**为什么不用内联 `style="color:#FF4757"`？** 因为：
1. 内联 style 优先级最高，难覆盖
2. 颜色值散落在 JS 里，改颜色要改两处
3. 用类名后，所有颜色集中在 `src/app.css` 的 `@theme` 块里，改一处全项目生效

### 7.4 模板函数

每个 `xxxTpl()` 函数返回一段 HTML 字符串。本质就是"数据 → HTML"的转换器。

#### `freshnessTpl(tradeDate)` —— 数据新鲜度标签

```js
function freshnessTpl(tradeDate) {
  var now = new Date();
  var pad = function(n) { return String(n).padStart(2, '0'); };
  var todayStr = now.getFullYear() + '-' + pad(now.getMonth() + 1) + '-' + pad(now.getDate());
  // todayStr 例如 "2026-07-25"

  var trade = new Date(tradeDate + 'T00:00:00');
  var today = new Date(todayStr + 'T00:00:00');
  var dayDiff = Math.round((today - trade) / 86400000);

  var label, cls;
  if (dayDiff === 0)        { label = '今日数据';     cls = 'bg-[#d4edda] text-[#155724]'; }
  else if (dayDiff === 1)  { label = '昨日数据';     cls = 'bg-[#fff3cd] text-[#856404]'; }
  else if (dayDiff <= 3)   { label = dayDiff + '日前数据'; cls = 'bg-[#ffe8a3] text-[#856404]'; }
  else                     { label = dayDiff + '日前数据'; cls = 'bg-[#f8d7da] text-[#721c24]'; }

  return '<span class="inline-block px-2.5 py-0.5 rounded-full text-xs font-bold ' + cls + '">' + label + '</span>';
}
```

**关键知识点**：
1. `new Date()` 拿当前时间。`getMonth()` 返回 0-11，所以要 +1。
2. 两个 Date 相减得到**毫秒差**，除以 `86400000`（一天的毫秒数）得到天数差。`Math.round` 防夏令时带来的小数误差。
3. `'T00:00:00'` 是为了本地时间解析（避免 UTC 偏移让日期差一天）。
4. 4 档新鲜度对应 4 种背景色 + 文字色：绿（最新）→ 黄 → 橙 → 红（最旧）。

#### `headerTpl(data)` —— 页头

没什么新东西，就是把 `freshnessTpl` + 数据日期 + 更新时间组合起来。注意它**调用**了 `freshnessTpl()`——模板函数可以互相组合。

#### `marketCardTpl(m)` —— 市场概况卡片

```js
function marketCardTpl(m) {
  function row(label, value, valueClass) {
    return '<div class="flex items-baseline ...">' +
      '<span class="...">' + label + '</span>' +
      '<span class="font-mono ... ' + valueClass + '">' + value + '</span>' +
    '</div>';
  }
  function col(rows) {
    return '<div class="flex flex-col gap-2">' + rows.join('') + '</div>';
  }
  return '' +
    '<div class="bg-white border ... flex flex-col sm:flex-row sm:justify-between gap-4 ...">' +
      col([row('上涨：', esc(m.up), 'text-up'), ...]) +
      col([row('总成交额：', esc(m.totalAmount), 'text-brand'), ...]) +
      col([row('价格中位数：', esc(m.priceMedian), 'text-brand'), ...]) +
    '</div>';
}
```

**关键设计**：
1. **内部嵌套函数** `row()` / `col()`：只在 `marketCardTpl` 内部用，所以定义在内部不暴露到外面。这叫"作用域封装"。
2. **DRY 原则**（Don't Repeat Yourself）：3 列 × 3 行 = 9 行结构完全一样的卡片，用 `row()` 函数避免写 9 遍。
3. **`rows.join('')`**：把数组拼成字符串，比 `+=` 拼接性能好。
4. **响应式布局**：`flex-col sm:flex-row` —— 手机上竖排，平板以上横排。
5. **颜色作为参数传入** `row(label, value, valueClass)`：让同一个 `row()` 能渲染红/绿/紫不同颜色的值。

#### `tableTpl(tableData, colWidths, stickyCount)` —— 表格（最复杂）

这个函数较长，分几段讲。

**第 1 段：算 bar 图最大值**

```js
barColIndices.forEach(function(idx) {
  var label = colLabels[idx] || '';
  var isBidi = label.indexOf('涨幅中位数') >= 0;
  var maxVal = 0;
  rows.forEach(function(r) {
    var v = r[idx];
    if (v === '-' || v === '') return;
    var n = parseFloat(String(v).replace('%', ''));
    if (!isNaN(n)) maxVal = Math.max(maxVal, isBidi ? Math.abs(n) : n);
  });
  maxBarValues[idx] = maxVal || 1;
});
```

要画 bar 图，必须知道"最大值"才能算出每条 bar 的相对宽度（百分比）。

- `isBidi`（bidirectional 双向）：涨幅中位数有正有负，用绝对值找最大。
- 普通列（如成交额）：只有正值，直接找最大。
- `maxVal || 1`：防除零，万一全列都是 `-`，避免 `NaN`。

**第 2 段：算冻结列宽度与左偏移**

```js
var frozenWidths = [];
for (var i = 0; i < stickyCount; i++) {
  frozenWidths.push((colLabels[i] || '') === '排名' ? 36 : 0);
}
var leftOffsets = [];
var acc = 0;
for (var i2 = 0; i2 < stickyCount; i2++) {
  leftOffsets.push(acc);
  acc += frozenWidths[i2];
}
```

"冻结列"就是表格横向滚动时**固定在左边不动**的列（用 `position: sticky`）。只有"排名"列固定 36px，其他冻结列宽度为 0（自适应）。

`leftOffsets` 算的是每一列相对左边的偏移：
- 第 1 列：`left: 0`
- 第 2 列：`left: 36`（第 1 列宽 36px，所以第 2 列要从 36px 开始）

**第 3 段：表头**

```js
var headCells = colLabels.map(function(label, i) {
  var isSticky = i < stickyCount;
  var baseClass = 'bg-brand text-white text-[13px] font-bold px-1 py-[7px] ...';
  if (isSticky) {
    var w = frozenWidths[i];
    var styleParts = ['left:' + leftOffsets[i] + 'px', 'z-index:3'];
    if (w > 0) { styleParts.push('width:' + w + 'px', 'min-width:' + w + 'px'); }
    return '<th class="' + baseClass + ' sticky" style="...">' + esc(label) + '</th>';
  }
  // 非冻结列：按百分比宽度
  var w2 = colWidths ? colWidths[i] * 100 : (100 / colLabels.length);
  return '<th class="' + baseClass + '" style="width:' + w2.toFixed(1) + '%">' + esc(label) + '</th>';
}).join('');
```

**关键知识点**：
1. `.map(...).join('')`：把数组转 HTML 字符串的经典写法。
2. **sticky 列必须设背景色**：否则滚动时下面的内容会"透"出来。这里用 `bg-brand`（紫色表头）。
3. **`z-index:3`**：表头 sticky 的 z-index 比表体 sticky（z-index:2）高，保证表头永远在最上层。
4. **冻结列用固定宽度（px），非冻结列用百分比**：固定宽度保证 sticky 不错位；百分比让表格自适应宽度。

**第 4 段：表体行**

```js
var bodyRows = rows.map(function(row, r) {
  var isAlt = r % 2 === 1;
  var rowBg = isAlt ? 'bg-surface-alt' : 'bg-white';

  var cells = colLabels.map(function(colName, c) {
    var val = String(row[c] != null ? row[c] : '');
    // 1. 判断对齐方式
    var isNumeric = /^[-]?[\d,]+\.?\d*%?$/.test(val.trim());
    var isCenter = !isNumeric || CENTER_COLS.indexOf(colName) >= 0;
    var alignClass = isCenter ? 'text-center' : 'text-left pl-2';

    // 2. 判断颜色：涨幅列红涨绿跌
    var colorClass = 'text-ink';
    if (c === changePctCol || colName === '涨幅') {
      colorClass = gainColorClass(val);
    }
    if (colName === '涨幅中位数') {
      colorClass = 'text-ink font-normal';
    }

    // 3. 判断字体：数字用 mono 字体
    var isMono = isNumeric || /^[\d.%\-]+$/.test(val.trim());
    var monoClass = isMono ? 'font-mono' : '';

    // 4. sticky 列特殊处理
    var stickyClass = '', stickyStyle = '';
    if (c < stickyCount) {
      stickyClass = ' sticky ' + rowBg;   // 关键：每行背景色不同，sticky 列要带上本行的背景
      stickyStyle = ' style="left:' + leftOffsets[c] + 'px;..."';
    }

    // 5. bar 图渲染（略，详见下文）
    var barHtml = ...;

    return '<td class="' + alignClass + ' ' + monoClass + ' ' + colorClass + stickyClass + ' ..." ' + stickyStyle + '>' +
      esc(val) + barHtml +
    '</td>';
  }).join('');

  return '<tr class="' + rowBg + '">' + cells + '</tr>';
}).join('');
```

**5 个细节，每个都有原因**：

1. **隔行变色**：`isAlt = r % 2 === 1`，奇数行用浅灰背景，提高长表可读性。
2. **数字右对齐 vs 文字居中**：默认数字右对齐方便比较位数，但本项目设计成全部居中——视觉简洁。
3. **涨跌色逻辑**：涨幅列才上色（红涨绿跌）；"涨幅中位数"列例外（保持中性，因为这列已经有 bar 图可视化）。
4. **sticky 列要带行背景色**：表头是紫色，所以 sticky 表头是 `bg-brand`；但表体每行背景不同（白/灰），所以 sticky 单元格要单独带 `bg-white` 或 `bg-surface-alt`，否则透出来很丑。
5. **数字用 mono 字体**：等宽字体（如 Consolas）让数字对齐，`131.471` 和 `664.88` 的小数点能对齐。

#### bar 图渲染（小段）

```js
if (barColIndices.indexOf(c) >= 0 && val !== '-' && val !== '') {
  var rawNum = parseFloat(String(val).replace('%', ''));
  if (!isNaN(rawNum)) {
    var isBidi = colName.indexOf('涨幅中位数') >= 0;
    if (isBidi) {
      // 双向 bar：从中间向左/右延伸，正向往右红，负向往左绿
      var ratio = Math.min(Math.abs(rawNum) / (maxBarValues[c] || 1), 1);
      var barW = 45 * ratio;
      if (rawNum >= 0) {
        barHtml = '<div class="bar-track-bidi"><div class="bar-right" style="width:' + barW + '%;background:#FF4757"></div></div>';
      } else {
        barHtml = '<div class="bar-track-bidi"><div class="bar-left" style="width:' + barW + '%;background:#2ED573"></div></div>';
      }
    } else if (rawNum > 0) {
      // 单向 bar：从左到右，金色填充
      var ratio2 = Math.min(rawNum / (maxBarValues[c] || 1), 1);
      var barW2 = 90 * ratio2;
      barHtml = '<div class="bar-track"><div class="bar-fill" style="width:' + barW2 + '%;background:#FDCB6E"></div></div>';
    }
  }
}
```

**为什么 bar 宽度用 inline style？** 因为宽度是数据驱动的（每行的 bar 宽度不同），Tailwind 工具类表达不了"任意 0-90% 宽度"。这是合理使用 inline style 的场景。

**45 和 90 的差异**：单向 bar 最长 90%（留 10% 边距），双向 bar 单侧最长 45%（两侧加起来 90%）。

#### `moduleTpl(title, subtitle, content)` —— 模块容器

```js
function moduleTpl(title, subtitle, content) {
  return '' +
    '<section class="mb-6 mobile:mb-4">' +
      '<div class="flex items-center justify-between h-9 mb-1.5 ...">' +
        '<h2 class="flex items-center gap-2.5 text-xl font-bold text-ink ...">' +
          '<span class="inline-block w-1 h-7 rounded-sm bg-brand mobile:h-[22px]"></span>' +
          esc(title) +
        '</h2>' +
        (subtitle ? '<span class="text-sm text-ink-medium ...">' + esc(subtitle) + '</span>' : '') +
      '</div>' +
      content +
    '</section>';
}
```

**关键设计**：
1. **`content` 作为参数**：这个函数是"通用容器"，里面塞什么由调用方决定。这就是**组合**：`moduleTpl` 提供"标题 + 副标题 + 内容框"，`tableTpl` 提供"具体表格"，组合起来用。
2. **紫色竖条装饰**：`<span class="inline-block w-1 h-7 ... bg-brand">`，用一个 4×28px 的紫色方块当模块标题的"项目符号"。简单粗暴但有效。

### 7.5 主渲染 `renderPage(data)`

```js
function renderPage(data) {
  var distColWidths = [0.18, 0.10, 0.08, 0.08, 0.16, 0.20, 0.20];
  var top10ColWidths = [0.05, 0.12, 0.08, 0.10, 0.14, 0.13, 0.13, 0.13, 0.12];

  var tableConfigs = [
    { title: '价格分布统计', data: data.tables.priceDist, widths: distColWidths, sticky: 1, subtitle: null },
    { title: '行业分布统计', data: data.tables.industryDist, widths: distColWidths, sticky: 1, subtitle: null },
    { title: '涨幅TOP10', data: data.tables.topGainers, widths: top10ColWidths, sticky: 2, subtitle: data.tables.topGainers.subtitle },
    { title: '成交额TOP10', data: data.tables.topAmount, widths: top10ColWidths, sticky: 2, subtitle: data.tables.topAmount.subtitle },
    { title: '跌幅TOP10', data: data.tables.topLosers, widths: top10ColWidths, sticky: 2, subtitle: data.tables.topLosers.subtitle }
  ];

  var tablesHtml = tableConfigs.map(function(cfg) {
    return moduleTpl(cfg.title, cfg.subtitle, tableTpl(cfg.data, cfg.widths, cfg.sticky));
  }).join('');

  return '' +
    headerTpl(data) +
    moduleTpl('市场概况', null, marketCardTpl(data.market)) +
    tablesHtml +
    '<footer class="...">免责声明...</footer>' +
    '<div class="...">共 ' + esc(data.bondCount) + ' 只可转债 | 数据自动更新中</div>';
}
```

**关键设计**：
1. **配置驱动**：5 张表的配置写在 `tableConfigs` 数组里。要加一张表？加一个对象就行。要改表标题？改数组里的字符串。**这就是"数据驱动渲染"**。
2. **`.map().join('')`** 把 5 个表的 HTML 拼成一个大字符串。
3. **结构清晰**：`headerTpl + moduleTpl(marketCard) + tables + footer`——一眼看出页面结构。

### 7.6 入口 `loadData()`

```js
function loadData() {
  var url = 'data.json?t=' + Date.now();
  fetch(url)
    .then(function(res) {
      if (!res.ok) throw new Error('HTTP ' + res.status);
      return res.json();
    })
    .then(function(data) {
      document.title = '可转债日报 - ' + data.tradeDate;
      document.getElementById('app').innerHTML = renderPage(data);
    })
    .catch(function(err) {
      document.getElementById('app').innerHTML = errorTpl(err.message);
    });
}

loadData();
```

**逐行讲**：

1. `var url = 'data.json?t=' + Date.now();`
   - 加时间戳查询参数，比如 `data.json?t=1784949372000`
   - **为什么？** 浏览器和 CDN 会缓存 `data.json`。加个时间戳让 URL 每次都不同，强制走服务器拿最新数据。这叫 **缓存破坏（cache busting）**。

2. `fetch(url)` —— 浏览器原生 API，发 HTTP 请求。返回 Promise。

3. `.then(function(res) { ... })` —— Promise 链。Promise 是"未来某个时刻会有结果"的对象，`.then()` 注册"拿到结果后做什么"。

4. `if (!res.ok) throw new Error(...)` —— `fetch` 即使 404 也走 `.then` 而非 `.catch`，所以要手动检查 `res.ok`（HTTP 2xx）。

5. `return res.json()` —— 把响应体解析成 JS 对象。也返回 Promise，所以下一个 `.then` 拿到的是 data。

6. `.then(function(data) { ... })` —— 数据到手，调 `renderPage(data)` 生成 HTML，塞进 `#app`。

7. `.catch(function(err) { ... })` —— 任何环节出错（网络挂、JSON 解析错、HTTP 4xx/5xx）都走这里，显示错误信息。

**为什么不用 `async/await`？** 可以的，但这是 IIFE 里写，需要包一层 `async function`。这里用 Promise 链更简单清晰。

### 7.7 最后的 `loadData();` —— 启动

IIFE 内最后一行 `loadData();` ——`app.js` 被 `<script src defer>` 加载并执行到这一行时，整个项目就"活"了。

### 7.8 为什么把 JS 从 HTML 拆出来

本项目最初把整段 JS 嵌在 `<script>` 标签里，后来拆成独立的 `app.js`。**为什么**：

1. **浏览器缓存命中**：用户每天看一次，HTML 几乎不变；JS 也几乎不变。独立成 `app.js` 后，浏览器可以缓存这个文件，二次访问只下载几百字节的 HTML + 走缓存的 JS + 加时间戳的 `data.json`。
2. **编辑器体验**：`.js` 文件能完整跑 ESLint、跳转定义、自动补全；内嵌在 HTML 里时编辑器把 JS 当"HTML 里的字符串"，语法检查弱。
3. **调试体验**：F12 → Sources 面板，独立 `app.js` 能直接看到、断点、单步；内嵌时显示为 HTML 一段，断点容易乱。
4. **关注点分离**：HTML 管"页面结构"，JS 管"渲染逻辑"，CSS 管"样式"——各管一摊，改一处不影响其他。
5. **可测试**：独立 JS 能被 Node.js 或 jsdom 直接 `require('./app.js')` 测试，本项目验证脚本就是这么做的。

**代价**：多一个文件 + 多一次 HTTP 请求。本项目文件都很小，HTTP/2 下完全可以忽略。

---

## 8. 端到端数据流回顾

```
用户打开 https://morecorianders.github.io/cbreport-web/
  │
  ▼
浏览器请求 index.html
  │
  ▼
解析 HTML，遇到 <link> → 请求 styles.css
解析 HTML，遇到 <script> → 执行 JS
  │
  ▼
JS 调用 loadData() → fetch('data.json?t=...')
  │
  ▼
服务器返回 data.json 内容
  │
  ▼
res.json() 解析成 JS 对象 data
  │
  ▼
renderPage(data) 被调用
  ├── headerTpl(data)           → 页头 HTML
  ├── marketCardTpl(data.market) → 市场卡片 HTML
  ├── tableTpl(...) × 5         → 5 张表 HTML
  └── footer                    → 页脚 HTML
  │
  ▼
拼接成一个大 HTML 字符串
  │
  ▼
document.getElementById('app').innerHTML = ...
  │
  ▼
浏览器把 HTML 渲染到页面 → 用户看到完整日报
```

---

## 9. 关键设计模式总结

| 模式 | 体现 | 为什么这么设计 |
|------|------|------|
| **关注点分离** | 配置 / 样式 / 结构 / 逻辑各在不同文件 | 改一处不影响其他，可维护 |
| **单一数据源** | 所有颜色集中在 `src/app.css` 的 `@theme` 块 | 改色只改一处，全项目生效 |
| **数据驱动渲染** | `tableConfigs` 数组定义所有表 | 加/改表只动配置，不动逻辑 |
| **模板函数组合** | `moduleTpl(title, tableTpl(...))` | 复用通用外壳，灵活组合内容 |
| **作用域封装** | IIFE 包整个脚本 | 不污染全局，不和别人打架 |
| **防御性编程** | `esc()` 转义所有动态数据 | 防 XSS 攻击 |
| **缓存破坏** | `data.json?t=时间戳` | 避免拿到旧数据 |
| **构建产物入仓** | `styles.css` 提交 git | GitHub Pages 无构建步骤 |
| **组件化样式** | `.bar-track` / `.table-wrapper` | 工具类搞不定的复杂样式抽成组件 |
| **响应式断点** | `mobile:` / `tiny:` | 一套代码适配 PC + 手机 + 小屏手机 |

---

## 10. 给初学者的学习建议

1. **先跑起来**：`npm install && npm run build && npm run serve`，浏览器打开看效果，建立"代码和效果"的对应。
2. **改一点试一点**：把 `src/app.css` 里 `--color-up: #FF4757` 改成 `#0000FF`（蓝色），跑 `npm run build`，刷新页面看涨幅色变蓝。**动手是最快的老师**。
3. **加日志看流程**：在 `loadData()` 的 `.then(function(data){ ... })` 里加 `console.log(data)`，用浏览器 F12 看真实数据结构。
4. **加一张表试试**：在 `data.json` 的 `tables` 里加一个新表（哪怕只有 1 行假数据），在 `tableConfigs` 数组里加一项配置——不用改 `tableTpl`，新表就能渲染出来。**这就是数据驱动的好处**。
5. **学新东西的顺序建议**：
   - HTML 基础（标签、属性、class）
   - CSS 基础（选择器、盒模型、flex、定位）
   - JavaScript 基础（变量、函数、数组、对象、字符串模板）
   - DOM API（`getElementById`、`innerHTML`）
   - Promise（`fetch`、`.then`、`.catch`）
   - Tailwind（看官方文档）
   - 响应式设计、CSS 优先级、XSS 防御……

---

## 11. 附：与 Tailwind v3 的对比（学习参考）

本项目最初用 Tailwind v3，后升级到 v4。**v4 是 2025 年 1 月发布的重写版本**，采用 Rust 引擎（Oxide），构建快 3-100 倍。最大变化是**配置从 JS 文件移到 CSS 文件**。这一章作为参考，帮助你理解为什么本项目现在的代码长这样。

### 11.1 文件结构对比

| 项 | v3 (旧) | v4 (新) |
|----|---------|---------|
| 配置文件 | `tailwind.config.js` (JS) | **删除**,配置写进 CSS |
| CSS 入口 | `src/input.css` | `src/app.css` |
| 安装包 | `tailwindcss` | `tailwindcss` + `@tailwindcss/cli` (CLI 拆分独立包) |
| 构建命令 | `tailwindcss -i src/input.css -o styles.css --minify` | `tailwindcss -i src/app.css -o styles.css --minify` (命令名相同) |

### 11.2 v4 配置怎么写——`@theme` 指令

v3 在 `tailwind.config.js` 里写 JS 对象:

```js
// v3
module.exports = {
  theme: { extend: { colors: { brand: '#8250DF' } } }
};
```

v4 在 CSS 里用 `@theme` 块,直接定义 CSS 变量:

```css
/* v4: src/app.css */
@import "tailwindcss";     /* 取代 v3 的三行 @tailwind 指令 */

@theme {
  /* 命名规则:--color-{name} → 自动生成 text-{name} / bg-{name} */
  --color-brand: #8250DF;
  --color-up: #FF4757;
  --color-down: #2ED573;

  /* --font-{name} → font-{name} */
  --font-sans: "Noto Sans CJK SC", sans-serif;

  /* --max-width-{name} → max-w-{name} */
  --max-width-report: 750px;
}
```

**关键差异**:
1. **三行 `@tailwind` 指令变一行** `@import "tailwindcss";`
2. **token 是真正的 CSS 变量**:打开浏览器 DevTools 能直接看到 `--color-brand` 这种变量,可运行时改值(实时换主题)
3. **没有 `extend` vs `theme` 的纠结**:v4 的 `@theme` 自动追加到默认主题,不存在"覆盖默认值"的问题

### 11.3 `@custom-variant` —— 自定义断点的新写法

v3 在 `tailwind.config.js` 里:

```js
screens: {
  'mobile': { raw: '(max-width: 600px)' },
  'tiny': { raw: '(max-width: 380px)' }
}
```

v4 在 CSS 里用 `@custom-variant`:

```css
@custom-variant mobile (@media (max-width: 600px));
@custom-variant tiny (@media (max-width: 380px));
```

定义后,`mobile:text-[21px]`、`tiny:px-2` 等工具类自动生效,用法和 v3 一样。

> ⚠️ v4 默认断点是 `min-width` 移动优先(`sm:` `md:` `lg:`)。本项目沿用 v3 的 `max-width` 桌面优先写法,所以必须用 `@custom-variant` 显式声明——不能用 `--breakpoint-*` 因为它只支持 `min-width`。

### 11.4 `@apply` 用法不变,但有新限制

`@apply` 在 v3 和 v4 里基本一样,都是"在 CSS 里借用工具类":

```css
/* v3 和 v4 都能这样写 */
.bar-track {
  @apply absolute bottom-0.5 left-1 right-1 h-1 bg-white rounded-sm overflow-hidden;
}
```

**v4 新限制**:不能 `@apply` 引用**同层的另一个自定义类**:

```css
/* v3 可以这样,v4 会报 "Cannot apply unknown utility class" */
.bar-track-bidi {
  @apply bar-track flex justify-center;   /* ❌ v4 报错 */
}

/* v4 必须展开写 */
.bar-track-bidi {
  @apply absolute bottom-0.5 left-1 right-1 h-1 bg-white rounded-sm overflow-hidden flex justify-center;
}
```

### 11.5 浏览器兼容性下降

v4 依赖现代 CSS 特性,**最低支持**:
- Safari 16.4+ (2023 年发布)
- Chrome 111+ (2023 年发布)
- Firefox 128+ (2024 年发布)

本项目是金融数据工具,用户基本都是现代浏览器,不构成问题。

### 11.6 v4 构建产物特征

打开 `styles.css` 你会发现所有自定义颜色都变成了 CSS 变量引用:

```css
/* v3 产物 */
.text-brand { color: rgb(130 80 223 / var(--tw-text-opacity, 1)); }

/* v4 产物 */
.text-brand { color: var(--color-brand); }
```

v4 的好处:**主题切换无需重建**。运行时改 `--color-brand` 的值(用 JS),所有用到 `text-brand` 的地方都会跟着变——这是 v3 做不到的。

### 11.7 升级过程踩到的坑

迁移时实际遇到的 2 个问题:

1. **`@apply` 引用自定义类报错**:把 `.bar-track-bidi { @apply bar-track ... }` 改成展开形式
2. **`@custom-variant` 语法**:不是 `&media (...)` 也不是 `media (...)`,正确写法是 `(@media (max-width: 600px))`——`@media` 必须用括号包起来

### 11.8 给初学者的迁移建议

- **不要手动迁移**:运行 `npx @tailwindcss/upgrade`,官方工具会自动处理 90% 的迁移
- **本项目是手动迁移**:因为配置简单、想给你展示 v3 vs v4 的差异
- **学 v4 的优先顺序**:看官方文档的 [Upgrade Guide](https://tailwindcss.com/docs/upgrade-guide),理解 `@theme` / `@custom-variant` / `@apply` 这 3 个新概念就够本项目用了

祝学习愉快！
